package com.study.javase.effective;

/**
 * 1.exceptions are, as their name implies, to
be used only for exceptional conditions; they should never be used for ordinary
control flow.
 * 2.A well-designed API must not force its clients to use exceptions for ordinary control flow
 *
 */
public class Item57_UseExceptionOnlyForExceptionConditions {

}
