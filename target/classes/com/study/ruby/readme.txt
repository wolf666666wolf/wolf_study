1. puts 比 print多了一个换行
2. irb可以进入命令行
3. ruby执行时 Ctrl+d结束命令
4. 本地变量 name="Zhangsan"
        实例变量@age=18
        全局变量   $name="zhangsan"
        常量 VERSION="1.3.2"
5. 带不带！， 带！直接修改原始变量
6. puts "Hello " + name  相当于  puts "#{name}", 但是只能用于单引号情况  ，单引号会原型输出
7. 船型表达式 <=>